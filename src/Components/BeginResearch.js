import React from "react";
import { Route, Redirect } from 'react-router';

const BeginResearch = () => {

    return (
        <div>              
            hello
    {/* here we need to insert also option to make "pilot\ pre test" so the user can check the system and see if the system and the sensor are alive */}
        </div>
    );
}

export default BeginResearch;